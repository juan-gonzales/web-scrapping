{
  "name": "mel-auth-desktop",
  "version": "0.1.0",
  "description": "Electron desktop app orchestrating MEL logins with Playwright",
  "main": "dist/electron.main.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only scripts/dev.ts",
    "start": "electron .",
    "build": "tsc && npm run copy-assets",
    "copy-assets": "mkdir -p dist/renderer && cp src/renderer/index.html dist/renderer/ && mkdir -p dist/config && cp src/config/app.json dist/config/ 2>/dev/null || true"
  },
  "dependencies": {
    "electron": "^28.2.0",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/node": "^20.8.10",
    "ts-node-dev": "^2.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
